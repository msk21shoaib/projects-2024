// click event = an interaction when a user clicks on a.we can respond to clicks by passing a callback to the onclick event handle
import Button from './Button.jsx'
import Pic from './Pic.jsx'

function App() {

  return(
    <>
      <Pic/>
    </>
  )
}

export default App
